# 决策助手 Roadmap

> **⚠️ 目标变更（2026-06-25）**：本产品不再追求规模化用户，**定位 = 作品集 + 简历 + 给朋友装逼，低成本优先**。
> 路线图据此瘦身为「**作品打磨轨**」，下方旧的生产化 P1/P2 表多数降级或不做。
>
> **保留（对简历/装逼加分）**
> - UI 精美打磨（第一观感）
> - 完整闭环：输入 → AI → 报告 → 复盘 → 画像（面试可讲、朋友会"哇"）
> - 线上可访问 + 可分享链接
> - 干净 GitHub README（带截图）+ 一页产品/技术复盘
> - Expo App **留作"我会跨端"的展示**：能跑 + 网页扫码下载 APK（debug 包即可），不上架、不做生产化
> - `packages/core`：保留（架构能力加分点）
>
> **不做（纯生产化、对目标无用）**
> - ❌ Apple App Store（$99/年，不办）
> - ❌ EAS 正式出包 / APK 分发基建（debug 包给朋友即可）
> - ❌ Sentry/错误监控、analytics、重测试（不追用户，无需）
>
> 下方原有 P0 已完成；P0.5/P1/P2 中除"作品打磨"相关项外，均视为**暂不做**。

## 作品打磨轨（当前活跃）

风格基调：**克制、高级感优先**（温暖但不幼稚）。

1. **小羊表情系统**：idle / thinking / happy / celebrate；关键时刻会反应（生成完→庆祝、空状态→招手、复盘完→点头眨眼）。先 Web。
2. **微动效（克制）**：眨眼、完成时轻弹、报告卡淡入。
3. **Web 布局/响应式打磨**：移动端导航、hero、报告页排版、间距统一。
4. **小羊同步到移动端**（RN-svg 镜像同一套表情）。
5. **收口**：README（带截图）+ 一页产品/技术复盘 + 网页扫码下载 APK 展示。

---

本文件原始内容（生产化阶段，多数已降级）。当前战略：**Next Web + Expo App + shared core**。

## 当前事实

- Web 已是 Next.js + Prisma + Neon Postgres，并部署在 Vercel。
- Expo App 重新启用，目标是 Android/iOS 原生体验。
- PWA 安装路线已放弃；manifest、service worker、离线页和安装提示已移除。
- Web 首页新增 Android App 下载入口，按钮和二维码都走 `/api/download/android`。
- P0 账号体系已实现：`User`、`UserSession`、`RateLimit`、`userId/anonId` 数据隔离。
- AI 接口走服务端 DeepSeek key + 身份/IP/全站日额度。
- 生产迁移 `20260624000000_add_accounts` 已部署到 Neon。

## P0 · 账号与数据隔离

| # | 问题 | 状态 |
|---|---|---|
| 1 | 没有账号/身份体系 | 已实现邮箱密码登录/注册 |
| 2 | 数据全局共享 | 已按 `userId` 或 `anonId` 隔离 |
| 3 | API 公开读写 | 决策 API 和页面已接身份过滤 |
| 4 | AI 只靠共享密码 | 已改为服务端代理 + 身份限额 |
| 5 | 无速率/额度限制 | 已加匿名/登录/IP/全局日限额 |

## P0.5 · Expo App 重新启用

| # | 任务 | 目标 |
|---|---|---|
| 1 | App auth 适配 | 已完成：服务端登录/注册返回 token；App 用 SecureStore 存 token，请求带 Bearer |
| 2 | 匿名 ID | 已完成：App 生成并保存 `anonId`，所有请求带 `x-anon-id` |
| 3 | App API client | 已完成：App 调现有 `/api/account/*`、`/api/decisions`、`/api/ai/*` |
| 4 | 历史同步 | 基本完成：登录/匿名优先读服务端，本地保留缓存兜底 |
| 5 | APK 出包 | 未完成：配置 EAS 或本地 Android build，产物链接写入 `ANDROID_APK_URL` |
| 6 | Web 下载入口 | 已完成：首页按钮和二维码指向 `/api/download/android` |

## P1 · Shared Core

| # | 问题 | 目标 |
|---|---|---|
| 1 | Web/App 类型重复 | 新建 `packages/core` 共享类型 |
| 2 | API 调用重复 | 共享 API client 和错误处理 |
| 3 | 决策报告结构漂移 | 共享 report parsing/normalization |
| 4 | 设计漂移 | 共享 colors/radii/spacing/typography tokens |
| 5 | 文案漂移 | 共享小羊暖语和报告分析语规范 |

## P1 · Web 生产化

| # | 问题 | 目标 |
|---|---|---|
| 1 | 缺真实使用数据 | 接 Vercel Analytics 或 Plausible |
| 2 | SEO/分享不完整 | favicon、OG 图、分享标题描述 |
| 3 | 缺隐私政策/服务条款 | 补页面，方便公开分发和上架 |
| 4 | 缺错误监控 | 接 Sentry 或等价方案 |

## P1 · App 生产化

| # | 问题 | 目标 |
|---|---|---|
| 1 | 缺 EAS 配置 | 补 `eas.json` 和 Android/iOS build profile |
| 2 | App 图标/启动图 | 规范化小羊图标和 splash |
| 3 | scheme/deep link | 配置 App scheme |
| 4 | 崩溃监控 | 接 Sentry |
| 5 | 上架材料 | 隐私政策、服务条款、截图、描述 |

## P2 · 质量与运维

| # | 问题 | 目标 |
|---|---|---|
| 1 | 自动化测试不足 | 覆盖生成、复盘、鉴权、身份过滤 |
| 2 | CI 门禁弱 | GitHub Actions 跑 lint/build/test |
| 3 | DB 备份策略未确认 | 确认 Neon 备份/恢复路径 |
| 4 | 可访问性未审计 | 对比度、焦点态、屏读标签 |
| 5 | 暗色模式未实现 | 低优先级 |
