# 决策助手进度账本

每个 agent 收工前更新本文件。只记录能帮助下一个人接上的事实。

## 开工入口

后续 agent 先读：

1. `docs/产品文档.md`
2. `docs/架构文档.md`
3. `docs/路线图.md`
4. `docs/进度账本.md`
5. `docs/运维手册.md`

收工前必须更新本文件，写清楚已完成、进行中、下一步、坑和验证结果。

## 当前状态

- 日期：2026-06-25
- 当前工作树分支：`main`
- 战略方向：Next Web + Expo App + shared core。
- 作品打磨状态：已完成。后续只做明确指定的小修。
- Next/Vercel 继续承载 Web、账号 API、Prisma/Neon、DeepSeek 代理。
- Expo App 重新启用，目标是 Android/iOS 原生 App。
- PWA 安装路线已放弃。
- 本地真实 `.env` 已存在但被 `.gitignore` 忽略；不要提交。

## 已完成

- P0 账号体系已实现并部署生产迁移：
  - `User`
  - `UserSession`
  - `RateLimit`
  - `Decision / DecisionReview / UserProfile` 的 `userId/anonId`
- Web 登录/注册/登出/session/claim API 已实现。
- 首页、历史页、详情页、画像页和决策 API 已按当前身份过滤。
- AI 生成、分析、画像更新接口已改为身份/IP/全站日额度。
- `/profile` 作为“我的”页，显示匿名/邮箱账号状态和登录/退出入口。
- Web 决策创建后进入 `/decisions/[id]/analyzing` 自动调用 AI，完成后跳转详情页。
- PC 顶部导航和移动底部导航已统一选中态规则。
- Expo App 已移除旧手填 AI 鉴权 UI 和共享鉴权 header。
- PWA manifest、service worker、离线页、安装提示已移除。
- Web 首页新增 Android App 下载卡片；按钮和二维码都指向 `/api/download/android`。
- `.env.example` 新增 `ANDROID_APK_URL`，移除旧 Basic Auth 示例变量。
- 服务端登录/注册现在会继续设置 Web cookie，并额外返回 App 可保存的 session token。
- `resolveIdentityFromRequest` 已支持 `Authorization: Bearer <token>`，App 和 Web 可共用同一套 `/api/*`。
- `/api/decisions` 已返回 App 可用的 options/review/解析后 aiAnalysis；`/api/decisions/[id]` 已补 GET 详情。
- Expo App 已新增 `expo-secure-store`，登录 token 存 SecureStore。
- Expo App 已生成并保存 `anonId`，所有 API 请求带 `x-anon-id`。
- Expo App 已新增登录/注册页；“我的”页显示匿名/邮箱状态并支持退出。
- Expo App 首页/历史页/结果页已优先读服务端，失败再读本地缓存；复盘提交走服务端 review API。
- Expo App 小羊已同步作品打磨 PRD 的 4 种 mood：`idle` / `thinking` / `happy` / `celebrate`。
- Expo App 小羊动效已接系统“减弱动态效果”：关闭循环浮动、眨眼和庆祝动效。
- Expo App 触发点已接入：首页问候 `idle`，生成页 `thinking`，空状态 `happy`，复盘保存成功 toast `celebrate`。
- Web 小羊已同步 4 种 mood：`idle` / `thinking` / `happy` / `celebrate`。
- Web 已补克制动效：浮动、眨眼、thinking 天线点脉冲、celebrate 轻弹和星点、报告卡淡入；`prefers-reduced-motion` 下关闭。
- Web 触发点已接入：首页 `idle`，分析页 `thinking`，空状态 `happy`，详情页推荐区 `celebrate`，复盘成功提示 `celebrate`。
- Web 响应式已收口：底部 tab 限定手机端，顶栏从 `sm` 起展示，首页 PWA 旧文案已改为 Expo Android 展示版。
- README 已新增，`docs/复盘.md` 已新增，下载卡已按作品展示文案打磨。
- README 已精简为 4 张高光图，其余截图进入 `<details>` 折叠块。
- README 已补 App 首页、生成中、报告页和记录页截图；完整报告长图放入折叠块。
- favicon 已新增：`app/favicon.ico` 和 `public/icons/favicon-64.png`。
- Web 首页下载入口已从 hero 中移出：hero 右侧只保留大号柔光小羊，APK 二维码和下载按钮单独放在 hero 下方卡片。

## 进行中 / 未完成

- `ANDROID_APK_URL` 需要在 Vercel 配置成真实 APK 地址。
- 还未配置 EAS Build / `eas.json`。
- 还未抽 `packages/core`。
- App 复盘编辑目前仍偏一次性提交；服务端 review API 尚未支持更新已复盘记录。

## 下一步建议

1. 配置 Android APK 出包：
   - 补 `eas.json`。
   - 产出 APK 后把下载地址写入 Vercel `ANDROID_APK_URL`。
2. 抽 `packages/core`：
   - types
   - API client
   - report normalization
   - design tokens
3. 统一 App/Web 复盘编辑能力：
   - 服务端 review API 支持更新已复盘记录。
   - App 结果页“修改复盘”提交到服务端。
4. 真机走查 Expo App：
   - 匿名生成报告。
   - 注册/登录后 claim 匿名记录。
   - 登录后 Web/App 历史互通。
5. 作品打磨已完成；后续只做明确指定的小修。

## 坑与注意事项

- 不要把真实 `.env`、Neon 连接串、DeepSeek key、APK 私有链接提交进 git。
- Expo App 不能直连 Neon 或 DeepSeek。
- Web 和 App 不需要 UI 完全一致；共享 tokens 和业务逻辑优先。
- 生产迁移已经完成，后续新增 migration 仍需用户确认后再跑生产库。
- 本地截图如果需要访问首页，必须保证本机能连 Neon；否则首页动态数据会 500，建议用线上环境截图。
